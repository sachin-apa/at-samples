state: "{{ inp_state }}"
raid_type: "{{ inp_raid_type }}"
name: "{{ inp_raid_name }}"
controller_id:  "{{ inp_controller_id }}"
drives:  "{{ inp_drives }}"
block_size_bytes:  "{{ inp_block_size }}"
capacity_bytes:  "{{ inp_cap }}"
optimum_io_size_bytes:  "{{ inp_optimum }}"
encryption_types:  "{{ inp_encryption_type }}"
encrypted: "{{ inp_encrypted }}"
