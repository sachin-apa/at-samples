attributes:
  BootMode: "{{ inp_boot_mode }}"
  OneTimeBootMode: "{{ inp_one_time_boot_mode }}"
  BootSeqRetry: "{{ inp_boot_seq_retry }}"
apply_time: "Immediate"
reset_type: "graceful_restart"
job_wait: true
job_wait_timeout: 1200
